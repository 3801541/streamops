{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "streamops.serviceAccountName" . }}
  labels:
    {{- include "streamops.labels" . | nindent 4 }}
{{- end }}
